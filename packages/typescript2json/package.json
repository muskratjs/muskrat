{
  "name": "typescript2json",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build": "rimraf ./dist/ && tsc",
    "lint": "tslint -c tslint.json 'lib/**/*.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^2.6.2",
    "tslint": "^5.10.0",
    "typescript": "^2.9.1"
  }
}
